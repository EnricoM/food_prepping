name: data
resolution: workspace
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  parsing:
    path: ../parsing
  collection: ^1.18.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  http: ^1.2.1


dev_dependencies:
  flutter_lints: ^3.0.1
